//   const dropdowns = document.querySelectorAll('#dropdownMenuButton');
//   const dropdownsArray = Array.from(dropdowns);
//   console.log(dropdownsArray);

//   const dropdownIds = dropdownsArray.map((element, index) => `dropdown-${index}` )

//   dropdowns.forEach((dropdown, index)  => dropdown.setAttribute('id', dropdownIds[index]));
//   console.log(dropdowns);


document.getElementById('alerts').innerHTML = "<%= j render 'shared/flashes' %>"
$('#dropdownMenuButton').dropdown('toggle');
